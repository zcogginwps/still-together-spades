{
  "name": "still-together-classics",
  "version": "0.2.0-beta.1",
  "private": true,
  "description": "Still Together - Classics: classic games built to be played while you're already on a FaceTime call together.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test test/*.test.js",
    "test:e2e": "node test/e2e.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
